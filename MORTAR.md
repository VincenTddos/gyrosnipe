# GyroMortar — MQTT over WebSocket 回合制砲擊對戰

舉起手機決定砲管仰角，長按蓄力放開發射。同時出手制：每回合所有人一起瞄準，
時間到齊射演算。單一 HTML 檔、無後端，房主瀏覽器負責所有彈道模擬與地形破壞。

---

## 怎麼跑

**本機測試**：用瀏覽器開 `mortar.html`，一個分頁按「開房」，其他分頁輸入房間碼加入。
沒有陀螺儀時用 ↑↓ 鍵或在砲管區域上下拖曳調整仰角、←→ 換方向、空白鍵蓄力發射。

**正式展示**：陀螺儀需要 HTTPS，丟到 GitHub Pages 後用手機開。
按「校準／啟用陀螺儀」授權後，**手機的俯仰角就直接是砲管仰角**——手機平放 0°、直立 90°，
不需要校準基準點，拿起來就會用。

操作：`◀ ▶` 切換射擊方向 → 傾斜手機調仰角 → **長按 FIRE**，力道條會來回擺盪，
放開的瞬間就是發射力道。送出後鎖定，等其他人。

---

## 為什麼這個設計不吃延遲

這是整份報告最核心的一段。

**關鍵在於交換的是什麼。** 第一版 GyroSnipe 傳的是「準心座標」——一種**連續狀態**，
每 50ms 就要同步一次，網路延遲直接變成手感延遲。實測公用 broker RTT 500ms，
準心在大螢幕上慢了快半秒，體驗崩壞。

GyroMortar 傳的是「離散提交」——一回合一次 `{方向, 仰角, 力道}`。瞄準階段有 15 秒，
指令晚 500ms 到達完全無感。**延遲被藏進回合邊界裡了。**

順帶的好處是流量暴跌：

| | 第一版（即時同步） | 第二版（回合制） |
|---|---|---|
| 訊息頻率 | 約 60 msg/s | 每回合約 30 則 |
| 15 秒內總訊息 | 約 900 則 | 約 30 則 |
| 其中決定勝負的 | 全部 | 4～8 則 |
| 延遲 500ms 時 | 幾乎不能玩 | 完全無感 |

兩邊的統計面板都是實測數字，可以直接截圖放簡報。

---

## MQTT Topic 設計

命名空間 `ntub-iot-gyromortar/{房間碼}/`

| Topic | QoS | Retained | 說明 |
|---|:---:|:---:|---|
| `up/join/{id}` | 1 | ✗ | 報到（兼 6 秒心跳，重連自動補報到） |
| `up/aim/{id}` | **0** | ✗ | 瞄準預覽，值有變才送，最快 5Hz |
| `up/fire/{id}` | **1** | ✗ | 本回合射擊參數，一回合一次 |
| `up/ping/{id}` | 0 | ✗ | RTT 量測 |
| `up/leave/{id}` | 1 | ✗ | **LWT** — 手機斷線房主立刻知道 |
| `dn/host` | 1 | ✓ | **LWT** — 房主 online / offline |
| `dn/meta` | 1 | ✓ | 回合階段、倒數、風力、玩家 HP 與提交狀態 |
| `dn/event` | 1 | ✗ | 命中、擊倒、回合開始、遊戲結束 |
| `dn/pong/{id}` | 0 | ✗ | RTT 回應 |

房主用單一 wildcard 訂閱 `up/#`。

### QoS 的切分就是這個系統的設計重點

大螢幕要顯示「每個人正在瞄哪裡」，這件事本身是連續狀態——照理說會把延遲問題帶回來。
解法是把訊息分成兩類，用 QoS 明確區分：

| | 瞄準預覽 `up/aim` | 發射指令 `up/fire` |
|---|---|---|
| QoS | **0** | **1** |
| 掉了會怎樣 | 沒事，下一則 0.3 秒後就到 | 該玩家這回合等於沒出手 |
| 晚 500ms 到 | 看不出來 | 看不出來（有 15 秒） |
| 頻率 | 值有變才送，最快 5Hz | 一回合一次 |
| 每回合則數 | 10～25 | 4～8 |

**判準是「掉了會不會影響勝負」**，不是「重不重要」。預覽很重要（沒有它玩家不知道自己瞄哪），
但它掉了不影響結果，所以用 QoS 0；發射指令一則都不能掉，所以 QoS 1。
統計面板把兩者分開計數，就是為了讓這件事在展示時看得見。

跟第一版對比也很值得講：第一版的高頻狀態流是**遊戲本身**（準心位置就是勝負），
所以延遲直接毀掉體驗；第二版的高頻流只是**視覺輔助**，勝負完全由回合邊界的離散指令決定。
同一組技術，因為遊戲設計不同，對可靠性與即時性的需求完全不一樣。

---

## 遊戲規則

- 2～8 人混戰，各自佔地形上一個位置，HP 100
- 每回合 15 秒瞄準，**所有人都提交後立刻演算**，不用等滿
- 齊射：所有砲彈同時飛，受重力與當回合風力影響
- 爆炸半徑 46px，直接命中約 46 傷害，距離越遠越少
- 爆炸會在地形挖出坑洞，地形改變後所有人重新落地
- 分數 = 造成的傷害 + 擊倒 40 分
- 12 回合或剩最後一人時結束

風力每回合隨機，畫面上方會顯示方向與強度——這是主要的策略變數，
同樣的角度力道在不同風向下會差到 160px。

---

## 給 iPhone 使用者的處理

- **不會自動暗掉**：用 Screen Wake Lock API 鎖住螢幕，舉著手機瞄 15 秒不會睡著；
  切到背景再切回來會自動重新申請。
- **瀏海與底部 Home 指示條**：版面用 `env(safe-area-inset-*)` 留白，FIRE 鍵不會被壓到。
- **iOS 沒有震動 API**：`navigator.vibrate` 在 iOS 上無效，所以改用 WebAudio 做聲音回饋
  （蓄力提示音、發射聲、爆炸聲）。音效在使用者第一次點按時解鎖，沒有外部檔案。
- **橫拿也能用**：直向時用 `beta` 判定俯仰角，橫向時自動改用 `gamma`；
  但橫向手感較差，所以偵測到橫向會蓋一層提示請玩家轉直。
- **權限**：`requestPermission()` 必須由使用者手勢觸發，所以綁在「校準／啟用陀螺儀」按鈕上。
  沒授權或沒陀螺儀時，砲管區域可以上下拖曳調整仰角，不會卡死。
- 按鈕加了 `touch-action: manipulation`，避免連點被判成雙擊縮放。

## 遊戲體驗優化

- **大螢幕顯示每個人的瞄準預覽**：砲管跟著轉，並畫出該玩家的預測彈道（含風力），
  只畫前 72%、尾端淡出——知道自己瞄哪裡，但落點還是要自己判斷。
- **已發射計數**：畫面右上顯示「2/4 已發射」，最後 5 秒倒數變紅並有提示音。
- **上一發參數記憶**：手機顯示「上一發 ▶ 62° · 力道 78」，方便下一發微調。
- **上一回合彈道殘影**：淡淡留在畫面上，可以看著它修正。
- **浮動傷害數字**與爆炸音效。
- 送出後砲管與預覽線變實線，代表已鎖定。

## 物理參數（程式最上方可調）

```js
const G = 0.28;            // 重力
const POWER_K = 0.18;      // 力道 → 初速
const WIND_MAX = 0.03;     // 風的水平加速度上限
const BLAST_R = 46;        // 爆炸半徑
const BLAST_DMG = 46;      // 直接命中傷害
const AIM_S = 15;          // 瞄準秒數
const MAX_ROUNDS = 12;
const PREVIEW_FRAC = 0.72; // 預覽彈道畫幾成（改成 1.0 就會直接看到落點）
```

實測射程（45 度）：力道 40 → 183px、力道 70 → 560px、力道 100 → 1146px。
場寬 960px，所以滿力可以打到全場任何位置，但要精準命中得算風。

人多的時候建議 `BLAST_R` 調小到 38、`AIM_S` 縮到 12 秒。

---

## 技術重點整理（給報告用）

1. **遊戲設計決定網路需求**——不是先選技術再硬塞遊戲。同樣是 MQTT over WebSocket，
   即時同步的射擊遊戲會被 500ms 延遲毀掉，回合制砲擊完全不受影響。
2. **量測驅動的設計修正**——第一版做出來、量到 RTT 500ms 與 60 msg/s、判定不可行、
   改設計。這條敘事線比直接做出成品更有說服力。
3. **QoS 策略隨設計改變**——第一版高頻狀態用 QoS 0，第二版全部 QoS 1。
4. **Retained + LWT** 處理中途加入與斷線偵測，兩版共用。
5. **房主權威**——玩家只送三個數字，彈道、傷害、地形破壞全在房主端算，天然防作弊。
6. **確定性物理**——同樣的參數在任何機器上跑出同樣的結果，這是回合制網路遊戲的基礎。

---

## 已知限制

- 預設連公用 broker `broker.emqx.io`，任何人都能連。正式展示建議自架或換帳密制 broker。
- QR 圖片來自 `api.qrserver.com`，被擋的話房間碼仍會顯示，手動輸入即可。
- 地形用高度圖表示，所以炸不出「洞穴」這種一個 x 對應多段地表的結構——
  地下爆炸會把上方整塊削掉。這個限制與改進方向也可以寫進報告。
